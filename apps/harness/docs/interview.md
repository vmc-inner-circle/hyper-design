작업 flow
레퍼런스 찾기 -> 비슷한 화면 참고 -> 내가 원하는 방향의 디자인(인터뷰 필요함), 컴퍼넌트 별로 원하는 느낌을 가져온다(일부만 참고) but 없으면화면을 구상한다.-> 먼저 ai 에게 구상을 하도록 하고 direction 을 주면서 한다.

이슈
디자인 시스템을 잘 안 따름 => 완적 작은 단위로 진행을 한다. 한 번에 하나를 깍기 보다 작은 단위로 한다. 
위치를 모르기에 그 위치를 알 수 있게 선택 가능하게 하고 거기에 피드백을 주는 형태로 


아이콘 (자체검증 필요함)
- 정규화 lucid icon 
- 사이즈별로 정해야함.

썸네일 이미지 
- 컨테인으로 or 아니면 썸네일 크기에 맞춰서 
- 클릭 시 배경 테두리리 마진


버튼 배경 색상